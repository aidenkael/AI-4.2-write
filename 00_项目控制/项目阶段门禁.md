# AI-write 项目阶段门禁

> 当前 Gate、允许动作、退出条件和禁止范围的权威入口。更新时间：2026-08-12。

## 1. 当前 Gate

**G4｜创作上下文与作者决策最小闭环：ACTIVE / G4-D。**

G4-A/B/C 已完成；G4-D **技术验证完成候选，待作者确认**；G4-E 未开始。

## 2. G4-D 已完成的真实验证

真实作者没有使用固定命令，只表达：“C 可能更对应这个场景”，并补充小说写法、节奏与详略不应固定。

系统处理：

1. 识别为 `modify`，不是机械 `choose C`；
2. 生成 `author_decision:g4d-dec-001`；
3. 只对未来 `approved_plan.plan.next_scene` 生成 `creative_change`；
4. `state_rev: 1 → 2`；
5. Canon / 已发生事件 / 人物状态 / 关系状态均不改变；
6. 不从作者原话自行发明具体审计、处罚或世界规则；
7. 不把 C 或某一种节奏写法升级成长期模板。

Decision：`06_工作区/G4B_沙盒_雾港档案室/g4d/decisions/g4d-dec-001.md`。  
Diff：`.../g4d/diffs/g4d-diff-001.yaml`。  
验证：`.../g4d/G4D_VALIDATION.md`。

## 3. 分支保护

无 authority 模拟语句已检查：

- `choose`：真实输入时才可能形成 author decision；
- `reject_all`：不写 Story State；
- `defer`：不写 Story State；
- `ambiguous`：不写，必要时最小澄清。

模拟输入不能冒充作者决定。

## 4. G4-D 完成条件状态

- 自然语言作者输入：✅
- choose / modify / reject_all / defer 最小语义分支：✅
- 真实作者反馈形成可追溯 Decision Record：✅
- State Diff 分类与权限正确：✅
- revision / source / trace 完整：✅
- approved_plan 可被作者改变而不冒充 Canon：✅
- 模拟输入无 authority：✅
- 作者不需要操作后台工件：✅

因此 G4-D 可标记为**技术验证完成候选**，等待作者确认是否进入 G4-E。

## 5. 当前禁止

- 未确认前自动进入 G4-E；
- 把单场景 C 方向升级成 Production Rule 或长期模板；
- 因“节奏多变”新增长期复杂 Schema/Skill；
- 在没有已接受正文时制造 `accepted_text`；
- 修改 Retrieval / RAG / KG / BKP / BookDistill；
- 完整 Writer / Reader / Critic / Editor；
- 正式长篇正文。

## 6. Git 安全

无明确授权禁止：`reset / restore / clean / force push / rebase / merge`。

历史 local dirty / untracked / `stash@{0}` 不自动恢复或清理；本阶段不为此建立临时分支。

## 7. 防跑偏

**案例只暴露问题，不决定架构。** 本轮关于节奏和详略的作者反馈，用来防止方案模板化，不据此追加一套新的节奏体系。
