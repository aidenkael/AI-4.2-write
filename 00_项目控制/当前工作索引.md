# 当前工作索引

> 唯一当前状态概览入口。更新时间：2026-08-12。

## 当前状态

**G4｜创作上下文与作者决策最小闭环：ACTIVE / G4-D。**

- G4-A：完成；
- G4-B：完成；
- G4-C：完成；
- **G4-D｜Author Decision Loop：技术验证完成候选，待作者确认**；
- G4-E：未开始。

## G4-D 已验证

沙盒：`06_工作区/G4B_沙盒_雾港档案室/`

真实作者只用自然语言反馈：“C 可能更对应这个场景”，同时指出小说写法、节奏和详略不应固定。

后台正确处理为：

- `author_action: modify`；
- 当前场景计划偏向 C 的效果目标；
- 不把 C 固化成通用模板；
- 不把“节奏多变”扩写成新的固定规则或手册条款；
- 不擅自新增具体处罚/审计等 Canon。

Decision：`g4d/decisions/g4d-dec-001.md`  
Diff：`g4d/diffs/g4d-diff-001.yaml`  
验证：`g4d/G4D_VALIDATION.md`

`story_state.yaml` 已从 `state_rev=1` 更新到 `state_rev=2`，**只修改 future approved_plan**；`canon_facts / character_state / relationship_state / occurred_events` 未改变。

## 额外分支检查

使用无 authority 的模拟语句检查 choose / reject_all / defer / ambiguity；模拟输入均不能写 Story State。

## 当前结论

G4-D 已证明：作者无需理解 Decision Record、Diff 或状态表，只需表达创作感觉；后台可以区分“当前场景决定”和“不要模板化的泛化提醒”，并只写回应写的未来计划。

## 当前禁止

- 未确认前自动进入 G4-E；
- 把本场景 C 方向升级成长期创作公式；
- 把作者关于节奏多变的提醒扩成新 Schema / Skill taxonomy；
- 在无 accepted text 时做机械事实结算；
- 修改 Retrieval / BKP / BookDistill；
- 完整 Writer / Reader / Critic / Editor。

## Git 状态提醒

本地 `stash@{0}` 与既有 untracked 仍是历史保护状态，不自动 pop/drop/clean，不为此建临时分支。

## 一句话状态

> **G4-D 已用一次真实作者自然语言反馈证明“作者说感觉 → 后台理解 → 只改应改状态”可运行；当前等待作者确认是否进入 G4-E。**
