# 当前工作索引

> 唯一当前状态概览入口。更新时间：2026-08-12。

## 当前状态

**G4｜创作上下文与作者决策最小闭环：ACTIVE / G4-D。**

- G4-A：完成；
- G4-B：完成；
- G4-C：完成真实验证；
- **G4-D｜Author Decision Loop：进行中**；
- G4-E：未开始。

## G4-D 唯一目标

验证真实作者可以只用自然语言完成：

`接受 / 修改 / 全部拒绝 / 暂缓 → 系统正确理解 → Decision Record / State Diff → 只写回合法状态`

作者不需要填写字段、选择 Skill 或审批机械状态。

## 当前真实验证

沙盒：`06_工作区/G4B_沙盒_雾港档案室/`

作者决策面：`06_工作区/G4B_沙盒_雾港档案室/g4d/G4D_作者决策真实验证.md`

当前 `story_state.yaml` 仍为 `state_rev=1`。进入 G4-D 本身**不修改 Story State**；只有收到真实作者反馈并完成边界判断后，才可能生成 Decision Record / State Diff。

## 已成立的前置结论

- G4-B：创作现场可脱离聊天恢复；
- G4-C：小 Context 成立；跨书综合按问题自然发生；当前无需升级 Retrieval/RAG/KG；
- BKP / Context / AI candidate 不能直接成为 Canon authority。

## G4-D 边界

- 真实作者反馈才可成为 `author_decision` 来源；
- 模拟测试语句可验证解析分支，但不能获得写入权；
- `reject_all / defer` 默认不改 Story State；
- 重大创作变化需真实作者确认；
- 当前若只改变未来场景方案，最多更新 `approved_plan`，不得伪装成已发生 Canon；
- 无正文时不产生 `accepted_text` mechanical settlement；
- 不进入完整 Writer / Reader / Critic / Editor；
- 不自动进入 G4-E。

## Git 状态提醒

本地 `stash@{0}` 与既有 untracked 是历史保护状态，不自动 pop/drop/clean，不为 G4-D 建临时分支；需要时先识别内容再处理。

## 一句话状态

> **G4-D 已开始：下一步不是让作者操作系统，而是让作者像真实创作一样说一句感觉，验证后台能否正确理解并只写回应写的状态。**