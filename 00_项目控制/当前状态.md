# 工作台当前状态

**CURRENT_PHASE = REAL_WRITING_USAGE**

**PHASE_STATUS = AUTHOR_ACCEPTANCE_IN_PROGRESS**（作者验收修复中；未声明作者验收完成）

**PRODUCT_BASELINE = GO_WRITE_2_0_APPROVED**。Go Write 2.0 是正式产品基线：面向中文长篇小说作者、固定项目管理框架而允许工作内容结构动态生长，稳定作品页为「作品概览 / 作品地基 / 故事规划 / 正在写 / 故事地图 / 作品检查」，并将字数规划（总目标 → 卷/阶段预算 → 章节范围 → 实际字数）列为一等能力。该记录是产品批准，不是实现完成声明。

UI 1.0 保留为历史技术纵切/实现参考；其冲突的产品假设已 supersede，历史记录不删除。现有 authority 边界不变：AI 候选须作者接受才有 authority，未来规划不是 Canon，作者编辑先做影响分析并走安全 writeback 合同。

工作台已从开发验证期进入真实使用期。

## 方法知识资产生命周期与多源统一检索（2026-08-27）

**根不变量（已实现并自动化强制）：**

`一个作者面素材入口 → 按来源类型分派的提纯/蒸馏 → 02 来源绑定知识 + 04 已验证知识 → 一次 KnowledgeRetrieve → 一个混合 RetrievalPackage → 模型跨全部相关来源选少量知识 ref → 精确绑定包进入 Context → StoryPlan/StoryWrite/Review 消费时无需知道每条知识来自哪个存储。`

| 能力 | 结果 |
|---|---|
| METHOD_SOURCE 素材类型 | DONE：canonical ledger/intake 校验接受；分类 Agent 可返回；物理路由进现有 02_研究资料 区（不新增根目录、不迁移旧素材；类型 authority 始终是素材资产.json）；现有四类行为不变 |
| MethodPrepare | DONE：独立确定性预处理（无模型/无改写/无 OCR）→ 06_工作区/MethodPrepare/<asset>_<名称>/（full.md / sections/S####.md 行稳定 / structure.json 不虚构层级 / metadata 指纹 / 报告）；坏/不支持/结构不可靠输入 REVIEW/FAIL，绝不假 PASS；Local Only |
| MethodDistill | DONE：方法取向蒸馏（非 BookDistill 换标签）；确定性 validate/prepare → Agent 语义抽取（复用现有 Direct/Interactive 任务设施）→ 确定性定稿（拒重复 id/空 statement/非法 kind/断裂证据/过期指纹/加载器不可解析）；只在全部通过后写 FINALIZED_RETRIEVAL_READY；capability_candidate 绝不自动产生/晋升 05 侧 Skill；方法源绝不自动进 04 |
| 02/04 知识存储 | DONE：02 = 来源绑定外部知识（<asset>/bkp 布局不变 + <asset>/method 并存）；04 可调用包合同（identity/validation.md/knowledge/cards.md，仅 FINALIZED_VALIDATED 可检索；当前生产无包是合法状态） |
| KnowledgeRetrieve 统一多源 | DONE：一次 retrieve 返回单一混合包（reference_bkp / method_source / validated_knowledge）；不路由、不让模型选库；公共命中合同泛化（selection_ref = source_kind/source_id/source_anchor）；无向量库/无新模型调用/无 KnowledgeRouter |
| 选择管道泛化 | DONE：selected_knowledge_refs / selected_knowledge_hits / _MAX_KNOWLEDGE_HITS / selection_ref 替换 BKP 专属字段（StoryPlan/StoryWrite/Review/NewProject/ContextCompiler/审计/任务模板）；快照升版 v2，旧版 fail closed；P0 绑定不变量与零检索路径保持 |
| Materials 单一作者面 | DONE：仍是单一素材页；类型标签「方法/技巧资料」；按钮作者面「提纯/蒸馏」；UI 只传素材 id，后端 prepare_material/distill_material 按类型分派；定稿方法包 → writing_callable=true；无新导航/无来源选择器 |
| 审计 | DONE：检索事件记录通用 selection_ref/source_kind/source_id（含混合来源集合），Context 事件记录选中知识 refs；不记录知识正文/完整 prompt/正文/token/凭据 |
| Post-action | DONE：MethodPrepare 成功后只刷新三份 material state files（产物留 06 Local Only）；MethodDistill 只提交当前资产 method/ 子树 + 三份文件；复用 MaterialIntake post_action，无新 Git 机制 |
| 测试 | 后端 301 passed / 2 skipped；MethodIntake/MethodPrepare/MethodDistill/KnowledgeRetrieve 多源新套件全部通过（含混合检索三包共存证明与绑定拒绝证明）；现有已定稿参考 BKP 回归保持；UI `tsc -b` + `vite build` PASS；`npm run test:model` 13 passed；零真实模型调用 |

**记录（REPORT ONLY）**：
- 本次是任务书显式授权的定向重开：仅方法类资产生命周期 + 检索身份/目录 + 最小 Context 选择管道 + Materials 路由/UI + 审计 + 直接受影响文档；其余冻结的文学/authority 语义保持冻结。
- 旧决策「当前不开发 MethodDistill」已由本次任务书显式取代（长期开发手册第 2/3/13 节已根改）。
- 真实作者纵切仍未完成：方法书的首次真实提纯/蒸馏与真实模型抽取质量待作者实测；AUTHOR_FACING_ONE_SENTENCE_ENTRY 仍不得标记 proven。

## 作者任务生命周期与 HCI 修复（2026-08-27）

**根不变量（已修正并全量迁移）：**

`AI 任务属于 Go Write / App 级协调器，不属于挂载的页面组件。`

页面可以发起/展示/确认/取消任务，但导航/卸载绝不静默取消任务、孤立任务、阻止其后端推进、丢失其候选或丢失其项目身份。修复不逐页打补丁，而是先替换错误的生命周期归属一次，再把全部受影响操作迁移到修正模型。

| 修复项 | 结果 |
|---|---|
| App 级任务协调器 | DONE：`ui/src/features/tasks/AuthorTaskCoordinator.tsx` 挂在 Router 之上；拥有唯一活跃作者操作（kind / request_id / project_id / status / phase / 作者可读消息 / 非机密执行元数据 / 结果候选 / error / 目标页）；单一轮询会话（页面卸载后继续）；阶段要求作者动作通知一次、完成/失败通知一次（绝不伪造进度）；结果保留到确认/丢弃/页面显式消费；remount/reload 经 `get_active_author_operation()` 恢复 |
| 后端恢复真相 | DONE：`operations/author_operation.py` + Bridge `get_active_author_operation()`；只返回非机密事实；Interactive pending 可恢复；Direct 仅当 in-process worker 仍在才恢复，进程重启后孤儿请求 fail closed（绝不显示假 running） |
| 桥：请求存储与 /gowrite 激活分离 | DONE：`create_request(..., activate_for_gowrite=False)`；Direct 请求永不进入 Qoder active.json；只有 Interactive 显式激活；第二个 Interactive 请求触发稳定忙碌错误（`BridgeBusyError`），绝不静默覆盖 active.json；StoryWrite Stage 1→2 原地换任务、active 指针不变 |
| StoryWrite 交互两阶段可离页推进 | DONE：协调器（而非 WritingPage）持续调用 `get_story_write_request()`；作者离开写作页后第一次 `/gowrite` 仍被消费并推进到 `pending_prose`，全局宣布「上下文已准备好，请再次执行 /gowrite」；第二次 `/gowrite` 后候选保留，返回写作页可见；两阶段隔离与候选确认 authority 不变 |
| 导航不清除正式项目选择 | DONE：Router 移除 `projectSection==null → clearSelection()`；全局导航（设置/素材/灵感/首页）保留 `FormalProjectShell.selected`；打开另一正式作品显式替换选择 |
| 全局运行任务条 HCI | DONE：`TaskStrip` 紧凑持久条（操作标签 + 项目名 + 真实状态 + 主动作「返回任务」/「前往 Qoder 执行 /gowrite」+ 仅取消有效时显示「取消」）；完成/失败一个全局 toast；sound 偏好真实生效（WebAudio 提示音），autosave/compactMap 假偏好移除 |
| 五个页面控制器迁移 | DONE：useNewProjectController / useDevelopmentController / useWritingController / useReviewController / useMaterialsController 全部改为协调器消费者；页面本地只留输入/选中/只读面；移除页面本地轮询定时器与「unmount 即取消」清理 |
| 审计生命周期纠正 | DONE：NewProject/StoryPlan/StoryWrite 候选生成 ≠ 操作完成——新增非终态 `awaiting_confirmation`；确认后 `authority.confirmed` 物理存在于最终审计 JSON 且状态 `completed`；丢弃/取消 → `canceled`；Review/素材分类/蒸馏保持直接 `completed` |
| 执行记录 HCI | DONE：`重新检测` 只出现在 AI 服务/API、Agent、执行配置；执行记录独立 `刷新记录` + 有 running/awaiting_confirmation 时适度自动刷新；展开↔收起真实切换（收起关闭明细不再重拉）；明细 key 用 `event_id`（旧版回退 seq）；交互摘要绝不拼入 Direct 模型（模型未验证） |
| 写作/检查动作语义 | DONE：「给我几个方案」→ Development/StoryPlan 一次性预填（绝不生成正文）；「检查这段」→ 章节 session 交接，Review 消费一次并选中有效章节（绝不自动运行）；「接下来怎么写」保留为 StoryWrite 输入快捷方式 |
| 素材页一致性 | DONE：`素材库` = 整个 canonical 素材库（含可用于写作项，不再排除）；页面打开在「待处理」时执行一次确定性收件箱扫描；拖放 UI 改为诚实的「选择本地文件」（pywebview 6.2.1 无安全拖放路径 API，不假装支持拖放）；classify/distill 异步状态归协调器，离开素材页不丢 |
| 可见设置真值审计 | DONE：移除假的 autosave/compactMap 偏好与「保存界面偏好」空按钮；sound 偏好真实生效 |

**记录（REPORT ONLY）**：
- 协调器边界：不是工作流引擎/事件总线；一次只允许一个活跃 AI 操作（MVP 语义）；失败任务保留到页面消费或重试替换。
- 桥激活语义：`active.json` 只可能是 Interactive `/gowrite` 任务的指针；清理只清与自身 id 匹配的指针。
- StoryPlan 检索命令改为显式 `--request <request_id>`（与 StoryWrite/Review/NewProject 同一 P0 精确绑定），Direct 请求不再依赖 active 指针。
- 测试：后端 287 passed / 2 skipped（新增桥激活分离、审计 awaiting_confirmation→authority.confirmed、恢复 API、两阶段 active 保持等）；UI `tsc -b` + `vite build` PASS；前端纯模型测试 13 passed（node:test，`npm run test:model`）；frozen Skill 套件保持通过。
- 剩余真实 blocker：真实作者 acceptance 端到端（真实模型文学质量/工具调用/素材加工）未完成；自动化全部假 adapter / 假 CLI，零真实模型调用。

## 运行时审计 + 素材管线修复（2026-08-26）

完成一轮根因修复/接线（StoryWrite 交互桥、Harness 通用发现、验证式执行审计、素材真实管理/加工工作流；零真实模型调用）：

| 修复项 | 结果 |
|---|---|
| StoryWrite Interactive 两阶段桥 | FIXED：真实两次 `/gowrite`（pending_selection → 验收+Context 编译 → pending_prose → 候选）；两阶段隔离保留；取消/晚到响应/换项目防护；绝不回退 Direct |
| DeepSeek Harness 多 provider 发现 | FIXED：通用解析全部 `llm-pi-ai.providers` 并按 provider 分组（不硬编码 DeepSeek/Flash/Pro/token-plan）；受管默认单独标识；无凭据路由标不可选；精确 provider/model 走临时 settings + `--patch`，不改永久配置 |
| 验证式执行审计 | DONE：`gowrite_execution_audit/v1`，存储 `06_工作区/运行审计/YYYY-MM-DD/<request_id>.json`；只记录机械验证事件（operation/agent/bridge/skill/retrieval/context/candidate/authority）；审计失败绝不影响作者操作；Settings 新增「执行记录」只读面 |
| 素材与学习 → 真实工作流 | DONE：本地文件导入（先进 MaterialIntake 收件箱）→ scan → 确定性事实优先、无法定论才一次 Agent 分类（只允许 NEW_ASSET/ATTACH_EXISTING/REVIEW，校验扫描+台账）→ 作者确认 → 事务入库 → 显式「提纯」（真实 SourcePrepare CLI）→ 显式「蒸馏」（真实 BookDistill validate/prepare/Agent 阅读/assemble/profile/bkp）→ 只有 FINALIZED BKP 才标「可用于写作」 |
| 跨切面缺陷 | 审计不丢 finalize 事件（get 路径收尾）；direct worker 与 recorder 创建顺序修正；harness 包根解析不再依赖 bin.js 深度 |

**记录（REPORT ONLY）**：
- StoryWrite 交互桥：一次作者操作、同一请求生命周期；Stage 1 请求等待 `/gowrite`，Qoder 返回选择 JSON，Go Write 验收 + P0 绑定 + 编译精确 Context 快照后请求原地换成 Stage 2 任务；第二次 `/gowrite` 只执行正文生成（fresh Agent invocation），`context_ref` 校验；Stage 2 任务绝不含未选中 Story State。
- Harness 发现合同：受管默认 = `agent-default-model`（deepseek-official/deepseek-v4-flash）；可选模型按 provider 分组（当前本地 = qwen-token-plan-cn：DeepSeek V4 Flash / DeepSeek V4 Pro / GLM-5.2 / Qwen3.x 等）；执行用临时 settings.yaml + `--patch`，永久配置不变。
- 审计存储 = `06_工作区/运行审计`（无根级 08）；审计是机械可观测性，不是 AI 推理日志；不存 key/token/完整 prompt/完整输出/正文/BKP 卡全文。
- 素材生命周期：intake → SourcePrepare → BookDistill → finalized BKP → KnowledgeRetrieve 按需；SourcePrepare/BookDistill 是显式离线动作，绝不随页面加载运行。
- 剩余真实 blocker：真实作者 acceptance 端到端（真实模型文学质量/工具调用/素材加工）未完成；自动化全部假 adapter / 假 CLI，零真实模型调用。

## 作者验收修复（2026-08-25）

基于作者首次 3 步验收暴露的问题完成一轮 acceptance-repair（只修根因、不加超时掩盖、不新增产品模块、零真实模型调用）：

| 验收项 | 结果 |
|---|---|
| 新建作品确认后不再卡“正在打开作品” | FIXED：前端 stale-closure 根因（confirm 闭包读不到 setState 后的 confirmed）→ `confirm()` 返回结果驱动 reload/open |
| 设置页打开立即显示已保存配置，不重跑全量发现 | FIXED：后端拆分为已保存设置 + last-known 发现快照；仅显式“重新检测”强制刷新 |
| 能区分“已保存”与“环境检测” | FIXED：UI 显示已保存徽章 + 配置摘要 + 检测时间/来源 |
| Qoder 升级后交互桥按真实安装合同检测/修复 | FIXED：桌面端新路径（Qoder CN IDE / QoderCN）+ 双数据根命令位置 + 逐位置就绪事实 |
| Qoder 模型目录如实反映内置/自定义语义 | FIXED：`名称 (Provider) (provider/model)` 路由按 CLI 文档识别为自定义 modelID，单独分组 |
| DeepSeek Pro 只在真实可选时暴露 | FIXED：受管默认（profile 单模型）如实报 managed；Pro 经 pi-ai 自定义路由可选时如实报 selectable，不硬编码名称 |
| 素材页按作者意图组织 | FIXED：可用于写作 / 待整理 / 需更新 / 待入库；选中素材回答能否调用/为什么/下一步；无假动作 |
| 关键页面视觉回归 UI 1.0 | DONE：页面级 CSS + 基线类恢复（Home/Projects/Development/Materials/Settings），保留真实数据 |
| 无可见假/空动作 | DONE：修复灵感“标记已用于作品”无效果按钮、死代码分支、禁用无原因按钮 |
| 跨应用同类缺陷审计 | DONE：4 个异步操作 prepare 等待竞态、unmount 后台取消、Overview 项目切换过期结果丢弃等 |
| 测试/构建 | 后端 226 passed / 2 skipped；UI `tsc -b` + `vite build` PASS |
| 真实模型/API 调用 | 0 次（仅本机只读发现命令与本地测试） |

**记录（REPORT ONLY）**：
- Qoder 升级合同：CLI `~/.qoder-cn/bin/qoderclicn/qoderclicn.exe` v1.1.29；桌面 `Qoder CN IDE.exe` / `QoderCN.exe`；命令位置 = 安装版数据根 `.qoder` 与 `.qoder-cn`；`--model` 对自定义路由接收 modelID（`provider/model`）。
- DeepSeek Harness 能力：headless = 单受管默认（deepseek-v4-flash）；per-request 覆盖 = 临时 settings 副本 + `--patch`；自定义路由（含 DeepSeek V4 Pro）由 `settings.yaml` pi-ai providers 推导并可选。
- Settings 持久化/发现分离：`get_agent_settings` 复用 last-known 快照；`discover_agent_environment` 显式刷新；保存允许原样重存已保存模型。
- 素材作者面语义：可用于写作 = knowledge 已可用（可被 KnowledgeRetrieve 调用）；待整理/需更新 = 需要离线整理；页面打开零模型。
- 剩余真实 blocker：真实作者 acceptance 端到端（真实模型文学质量/工具调用）未完成；Qoder /gowrite 桌面端实际读取位置与 DeepSeek 自定义路由可调用性以真实使用为准。

## 子系统状态

| 子系统 | 状态 | 说明 |
|---|---|---|
| SourcePrepare | freeze / available | 原著标准化就绪，按需调用 |
| MethodPrepare | available（2026-08-27） | 方法/技巧资料确定性预处理就绪 |
| BookDistill | freeze / available | 参考作品知识提取就绪 |
| MethodDistill | available（2026-08-27） | 方法知识蒸馏与确定性定稿就绪 |
| KnowledgeRetrieve | available（统一多源，2026-08-27 升级） | 参考 BKP / 方法知识 / 已验证知识一次调用混合检索；不再是旧版 BKP-only 冻结实现 |
| StoryDesign | closed / frozen | 故事设计基础已验证并冻结 |
| StoryPlan | closed / frozen | 规划能力已验证并冻结（STORYPLAN_PHASE_CLOSED） |
| ContextCompiler | consumer-driven freeze | 上下文编译按需消费者驱动 |
| StoryWrite primitives | keep / freeze | 写作原语保持可用 |
| Mechanical settlement assist | keep / freeze | 机械结算辅助保持可用 |
| AUTHOR_FACING_ONE_SENTENCE_ENTRY | not yet proven | 新建作品纵切 PASS；完整入口仍需 StoryPlan / 正文 acceptance 纵切 |
| PRODUCT_BASELINE | GO_WRITE_2_0_APPROVED | 正式产品基线已批准；未实现的页面、动态结构和字数规划不得标为 DONE |
| AUTHOR_FACING_WORKBENCH_DESIGN | GO_WRITE_2_0_APPROVED / IMPLEMENTATION_PENDING | 产品基线已批准；实现仍仅允许真实接线所需最小改动与真实 bug 修复 |
| UI_1_0_BASELINE | TECHNICAL_VERTICAL_SLICE_REFERENCE | 历史技术/实现参考，不是正式产品基线 |
| UI_1_0_IMPLEMENTATION | FROZEN | Freeze Review PASS；UI code baseline = `c865ee75cfb5c71e5075771473a7bb8df384f991` |
| WRITER_PLATFORM_REQUIRED | no | 不需要完整写作平台 |

### UI 1.0 Freeze Review（2026-08-24；历史技术纵切记录）

`ready = YES`，`blockers = None`。

冻结实现共 11 个页面：Home / Projects / Project Overview / Development / Writing / Story Map / Project Data / Review / Materials / Ideas / Settings。

已确认：
- React 不直接调用 pywebview / backend / Skills；真实 Bridge 入口继续集中在 `ui/src/bridge/client.ts`。
- Mock 流程无隐藏真实 AI/API 调用。
- 两项目章节、构思候选、正文候选、Review、项目资料状态隔离。
- 项目内导航保持 activeProjectId，无强制硬编码项目切换。
- 新建项目、素材、AI 服务、项目资料、候选采用、正文采用、Review 处理等主要 Mock 流程可用。
- `tsc -b && vite build` PASS，0 errors / 0 warnings。
- UI 视觉方向与批准基线一致。

**UI Freeze 只冻结当时的技术纵切/实现参考，不代表 Go Write 2.0 产品基线或真实 Backend 接线完成。** 后续不得为了“未来可能需要”先重构 UI/AppStore；真实能力按具体 consumer 做最薄接线。

### 已验证的交互层事实（2026-08-20）

| 验证项 | 状态 | 说明 |
|---|---|---|
| NEW_PROJECT_IDEA_VERTICAL_SLICE | PASS | 「两条狗」案例：作者想法 → pending → /gowrite → JSON → proposal → 作者确认 |
| STORY_PLAN_BRIDGE_VERTICAL | PASS | 故事规划双执行路径：交互桥 /gowrite 保留；Direct 按 Settings 显式配置后台执行（非隐藏调用，绝不回退） |
| STORYPLAN_DIRECT_BINDING | PASS | Settings 保存的 Direct 执行模式 + Agent + 内置/自定义模型已绑定到 StoryPlan Author Operation（2026-08-25） |
| STORYPLAN_DIRECT_LIFECYCLE | PASS | Direct 后台执行 / 可轮询 / 可取消；prepare 不阻塞 API/UI；adapter.cancel() 真实终止运行中任务；取消后晚完成结果丢弃 |
| STORYPLAN_P0_REAL_TOOL_USE | NOT_REVERIFIED | P0 快照/指纹绑定机械可证（测试）；真实模型工具调用行为上次纵切未执行检索工具，未复验 |
| STORY_WRITE_BACKEND_BLOCKED | REPLACED | 旧"暂不可用"禁用路径已移除；StoryWrite 已接入 Direct 后台执行生命周期（见 STORYWRITE_DIRECT_BACKEND） |
| QODER_INTERACTIVE_BRIDGE | PASS | Go Write 准备任务 → 尽力切前台 → 作者 /gowrite → response 返回 |
| QODER_SESSION_IS_NOT_MEMORY | VERIFIED | 换干净 Qoder 会话仍可执行，Qoder 历史不是长期上下文来源 |
| STRICT_AGENT_JSON_ACCEPTANCE | PASS | Go Write 严格校验 Agent 返回 JSON，不合法则 rejected |
| HIDDEN_REAL_MODEL_TEST_CALLS | DISABLED_BY_DEFAULT | 默认 pytest 0 次真实模型调用；需 GOWRITE_REAL_QODER_TEST=1 显式开启 |
| NO_HIDDEN_AUTHOR_AI_CALLS | ENFORCED | 作者日常创作按钮不再后台偷偷调用 DeepSeek Harness / qodercli -p / BYOK |

## 已完成的验证链

1. SourcePrepare → BookDistill → BKP → KnowledgeRetrieve（参考知识链）
2. MethodPrepare → MethodDistill → 02/method → KnowledgeRetrieve（方法知识链，2026-08-27）
3. StoryDesign 运行底座与真实验证
4. StoryPlan 最小合同 → 真实长篇规划纵切 → 局部重规划（E2-A/B/C，STORYPLAN_PHASE_CLOSED）
5. Context Compiler 最小技术地基（E3-A）
6. StoryWrite 真实能力纵切（PASS_WITH_RESERVATIONS）
7. LongformContinuity 真实纵切（PASS_WITH_RESERVATIONS）
8. ThinStoryWrite 消费纵切（PASS_WITH_RESERVATIONS，NO_FURTHER_STORYWRITE_BUILD）

## 根目录结论

**ROOT_LAYOUT_FINALIZATION = COMPLETE**

一级目录职责已经确认并冻结：

- `00_项目控制`：系统状态、长期原则与开发手册；作者通常不操作
- `01_原始素材`：外部原始来源；作者投放/管理素材
- `02_素材知识库`：来源绑定、可追溯的参考知识；后台维护，作者按需查看
- `03_作品工程`：原创小说的设定、规划、正文与 production Story State；作者日常创作核心目录
- `04_写作知识库`：经跨作品与真实创作验证的长期写作知识；当前非日常操作目录
- `05_Skills与自动化`：已验证基座能力 / Skills；作者通常不操作
- `06_工作区`：可删除、可重建的临时运行与开发空间；作者通常不操作
- `07_工作台应用`：正式作者侧桌面应用（UI、接口、Agent 接入、应用层）；作者日常创作入口

作者日常通过 `07_工作台应用` 桌面入口操作作品与素材；直接目录操作以 `01_原始素材` 与 `03_作品工程` 为主；`02_素材知识库` 按需查看；`04` 当前无需日常管理；`00/05/06` 属后台。

一级目录编号表示职责/资产分层，不表示 runtime 执行顺序。没有真实使用问题时，不再重命名、重新编号或合并根目录。`07_工作台应用` 是 00~06 冻结规则的有理由扩展；不迁移现有 Skill、不迁移 `06_工作区/SourcePrepare`、不为目录整齐修改现有稳定路径。

## REAL_PROJECT_WIRING 生产合同

**REAL_PROJECT_WIRING_DESIGN = FROZEN**
**REAL_PROJECT_WIRING = NEW_PROJECT_VERTICAL_SLICE_PASS**
**REAL_PROJECT_WIRING_IMPLEMENTATION = REAL_VERTICAL_SLICE_PASS**

F0.1 contracts implemented and verified (2026-08-17):
- ProjectWorkspace production changed: author_intent required + frozen validate_author_intent, load_project frozen gates, persist_state_transition, acceptance settlement required + frozen apply_settlement gate
- STORYDESIGN_REAL_PROJECT_BINDING = TRUE (real frozen runtime call)
- STORYPLAN_REAL_PROJECT_BINDING = TRUE (real frozen runtime call)
- STATE_TRANSITION_PERSISTENCE = TRUE
- MULTI_PROJECT_ISOLATION = TRUE
- CROSS_PROJECT_WRITE_REJECTED = TRUE
- ACCEPTANCE_ALWAYS_USES_FROZEN_GATE = TRUE
- RECENT_PROSE_USES_FROZEN_STORYWRITE = TRUE
- SHARED_KNOWLEDGE_ROOT = TRUE / PROJECT_LOCAL_BKP_COPY = FALSE
- AUTHOR_INTENT_FROZEN_CONTRACT_VALIDATED = TRUE
- CONTROL_CHAR_COUNT = 0
- FROZEN_RUNTIME_PRODUCTION_CHANGES = 0

F0.2 final mechanical closure verified (2026-08-17):
- RECENT_PROSE_USES_FROZEN_STORYWRITE = TRUE (frozen prepare_recent_prose_window artifact)
- ACCEPTANCE_SCENE_REF_BOUND = TRUE
- CROSS_PROJECT_CANDIDATE_REJECTED = TRUE
- DUPLICATE_SCENE_REF_REJECTED = TRUE
- STATE_TRANSITION_REQUIRES_AUTHOR_DECISION = TRUE (full base equality, Canon unchanged, plan prefix, author_decision: prefix)
- ACCEPTED_PROSE_PARTIAL_WRITE = FALSE (fault injection CASE A + CASE B both rollback clean)
- PROJECT_PATH_CONTAINMENT = TRUE (resolve+relative_to for project_dir and chapter_path)
- MULTI_PROJECT_CONTEXT_ISOLATION = TRUE (real ContextCompiler integration)
- CONTROL_CHAR_COUNT = 0
- FROZEN_RUNTIME_PRODUCTION_CHANGES = 0

### 作者侧体验

作者不需要记 StoryDesign / StoryPlan / ContextCompiler / KnowledgeRetrieve / StoryWrite 名称，也不需要为每个 Skill 背固定 Prompt。作者只用自然语言表达：新建/继续/切换作品、构思、规划、写作、修改、确认方向、接受正文。Agent 负责识别操作并调用现有能力。

只有两类长期 authority 变化必须明确：

- 创作方向/规划确认：如“这个方向确定，就按这个走”
- 正文 acceptance：如“这版接受，定稿”

含糊表达不得自动升级为作者确认或 accepted_text。

### 每部作品独立

每部真实小说位于 `03_作品工程/<作品名>/`，拥有独立 `project_id`、Author Intent、Story State、approved_plan、Decision history、正式正文和项目专属资料。

多作品同时更新时：

- `02_素材知识库` / 未来 `04_写作知识库` 跨作品共享
- 原创 Context / State / Planning /正文绝不跨项目共享
- 切换作品必须切换完整 project context
- 任何会修改正文、State、planning、Decision 的动作，在唯一 `project_id` 未确定前禁止执行
- 多作品存在而用户只说“继续写”时不得猜；明确说“继续《作品名》”或“切到《作品名》”时直接解析

### 长篇正文与上下文

正式正文物理存储按章：`03_正文/第001章.md`、`第002章.md`……；只有作者接受的版本进入正式正文。

运行与 acceptance 使用稳定的 scene/write-turn ref；同一章可包含多个 accepted ref，不强迫小说采用固定场景结构。REAL_PROJECT_WIRING 需维护最小的 accepted ref → 正文章节映射，使 `accepted_text:<scene_ref>` 能追溯到正式正文。

每次写作不重读全文。上下文继续使用已冻结的分层策略：

- 长期连续性：Story State
- 未来方向：当前 active approved_plan
- 当前任务：ContextCompiler 只选择少量相关 State，空 selection 不 fallback 全 State
- 短期文字连续性：最近 accepted 正文末尾约 1000–2000 字 recent prose
- 久远具体原文：有真实需要时定向读取对应章节
- 参考写作知识：明确 knowledge need 时少量 BKP；0 BKP 合法

当前不建设原创正文全文 RAG、embedding、vector DB。

### acceptance 生产链

目标机械链：

`候选正文 → 作者明确接受 → 写入/更新对应章节 Markdown → 登记 accepted scene ref → mechanical settlement → 更新该作品 production Story State → 下一轮基于新 State + recent prose 继续`

未接受草稿不得写入正式正文，不得触发 production Story State 结算。

## 作者侧工作台

**AUTHOR_FACING_WORKBENCH_DESIGN = GO_WRITE_2_0_APPROVED / IMPLEMENTATION_PENDING**
**UI_1_0_BASELINE = TECHNICAL_VERTICAL_SLICE_REFERENCE**
**UI_1_0_IMPLEMENTATION = FROZEN**

- 07_工作台应用工程骨架已建立（desktop / ui / backend），Qoder 交互桥已通。
- 新建作品真实纵切 PASS（「两条狗」案例，2026-08-19）。
- 故事规划双执行路径已接线（2026-08-25）：交互桥 /gowrite（默认）保留；Direct 按 Settings 显式配置通过现有 Agent registry/adapter 后台执行（prepare 立即返回，可轮询/取消）。
- 正文写作已接入 StoryWrite Direct 后台执行生命周期（2026-08-25）：两阶段（选择 → 正文）独立 Agent 运行、精确检索包 P0 绑定、context_ref 绑定、异步轮询/取消。
- 正文写作 Interactive 两阶段交互桥已接入（2026-08-26）：真实两次 `/gowrite`（阶段 1 选择 → Go Write 验收+Context 编译 → 阶段 2 正文），两阶段隔离保留，绝不回退 Direct。
- 交互桥模式统一走 Qoder 桌面端 /gowrite；Direct 模式仅当 Settings 显式配置 Direct + 有效 Agent/模型时后台执行，绝不隐藏调用、绝不静默回退到其他 Agent/模型或交互模式。

### StoryWrite 执行状态（2026-08-25；UI 消费者 2026-08-25）

- **STORYWRITE_DIRECT_BACKEND = PASS**（prepare/get/cancel/confirm 生命周期 + 两阶段隔离 + P0 知识绑定 + context_ref；假 adapter 测试全绿）
- **STORYWRITE_INTERACTIVE = TWO_PHASE_PASS**（2026-08-26：真实两次 `/gowrite` 两阶段桥；阶段 1 验收 + 精确 Context 编译 → 阶段 2 正文；取消/晚到响应/换项目防护；绝不回退 Direct）
- **STORYWRITE_UI_CONSUMER = PASS**（WritingPage 已通过 `useWritingController` 消费真实后端生命周期（Direct + Interactive 两阶段提示）；正式 project_id 来自 FormalProjectShell，不使用 Mock activeProjectId）
- **STORYWRITE_ACCEPTED_PROSE_READ = PASS**（只读 `get_story_write_surface`：正式 03_正文 + accepted_text_index；path containment + SHA 校验；无写副作用）
- **STORYWRITE_MANUAL_EDITOR = NOT_CONNECTED**（手动编辑保存 / 新章节未接线：没有真实后端写合同前不做假保存、假时间戳、假章节）
- 真实模型 StoryWrite 质量/工具调用行为未验证（自动化测试无真实模型调用）。
- UI 1.0 11 页 Mock 实现 Freeze Review PASS；实现基线固定为 `c865ee75cfb5c71e5075771473a7bb8df384f991`，仅作历史技术纵切参考。
- 后续不再扩 UI 功能或做审美重构；真实接线只做具体 consumer 所需的最小 UI 改动。

### 正式项目外壳（2026-08-25）

- **FORMAL_PROJECT_SHELL = PASS**（`ui/src/features/projects/FormalProjectShell.tsx`：`listProjects()` 加载正式列表；`openProject({project_id})` 后端校验成功后才提交选择；Projects / ProjectLayout / Overview / Development / Writing 共享同一正式 project_id）
- **PROJECTS_REAL_CONSUMER = PASS**（ProjectsPage 只展示后端正式项目，无 Mock 章节/字数/更新时间/状态/简介；打开/继续写作先经后端校验）
- **PROJECT_OVERVIEW_REAL_CONSUMER = PASS**（Overview 只读 `getProjectOverview` + `getStoryWriteSurface`；无 Mock 回退）
- **STORYPLAN_BACKEND = PASS**（prepare/get/cancel/confirm 双执行模式：Interactive /gowrite + Direct 后台执行；P0 精确检索包绑定；proposal_noncanonical 候选；confirm 唯一 approved_plan 写入路径；已完成未确认候选可安全丢弃）
- **STORY_DEVELOPMENT_UI_CONSUMER = PASS**（DevelopmentPage 已通过 `useDevelopmentController` 消费真实 StoryPlan 生命周期；正式事实只来自 `getProjectOverview`；候选为后端单个只读候选，无伪造 A/B/C；确认后重载正式概览）
- **STORY_MAP_REAL_CONSUMER = PASS**（StoryMapPage 只读 `get_project_data` 正式 Story State 投影：人物/关系/事件/未解决线索/已确认规划；不编造关系边/日期/角色；关系不足时如实以卡片/列表呈现）
- **PROJECT_DATA_REAL_CONSUMER = PASS**（ProjectDataPage 只读 `get_project_data` 投影；零写回、零模型；无任意 Canon 编辑，改方向/事实走「故事发展」）
- **REVIEW_REAL_CONSUMER = PASS**（ReviewPage 只读 `get_review_surface` + 显式 `prepareReview / getReviewRequest / cancelReviewRequest`；单次 Agent 检查、Direct 后台执行、报告非权威零写回）
- **PROJECTS_NEW_PROJECT_UI = PASS**（ProjectsPage「新建作品」已接入真实生命周期：作品名 + 想法 → Generate → 后端单个候选 → 确认创建正式项目；确认后重载列表并进入概览）
- **IDEAS_REAL_CONSUMER = PASS**（IdeasPage 真实本地收件箱 `ideas.json`：列表/新增/删除/过滤 + 「帮我发展」项目内一次性预填）
- **MATERIALS_REAL_CATALOG = PASS**（MaterialsPage 真实 canonical 素材目录；页面加载只读零模型；刷新/扫描/入库均为显式动作，走 MaterialIntake 确定性契约）
- **MATERIALS_WORKFLOW = PASS**（2026-08-26：导入→待入库→确定性/Agent 分类→确认入库→显式提纯（SourcePrepare）→显式蒸馏（BookDistill）→FINALIZED BKP→可用于写作；页面加载零模型/零处理）
- **EXECUTION_AUDIT = PASS**（2026-08-26：`gowrite_execution_audit/v1`；存储 `06_工作区/运行审计`；机械验证事件；审计失败不影响作者操作；Settings「执行记录」只读面）
- HomePage 已是真实仪表盘：真实项目列表 + 快速灵感真实写入；无假章节/字数/更新时间/最近活动/示例书名。
- `ui/src/mock/`（data.ts / service.ts）与 `MockFormDialog` 已移除（重命名为 `FormDialog`）；AppStore 收缩为 UI/会话关注点。

## blind #2 复现结论（2026-08-24）

**BLIND_2_CLASSIFICATION = DETERMINISTIC_DEFECT**

原始 blind #2 本身不是正式 runtime/authority 污染：T2 未消费 T1 实际 BKP，而是使用裸 K 编号自行编写知识卡身份，并额外引用不存在的 K016；随后 Context 按真实 BKP anchor 注入了真实卡内容。因此原始案例的主要根因属于模型/检索生成层 identity mismatch，而不是“伪造卡内容被 runtime 当真”。

但 synthetic collision 证明了独立的确定性完整性缺陷：当不同 `book_id` 同时存在相同裸 `source_anchor`（例如 `book_a/K001` 与 `book_b/K001`），`selected_knowledge_ids=["K001"]` 会把两个 hit 静默同时注入 Context，没有歧义拒绝或 gap。

其余守卫均正常：
- missing ref 不注入，并产生 BKP gap；
- project A planning token 不能确认到 project B；两边正式状态保持不变；
- canceled request、replay、stale state 均被拒绝/失效；
- 调查未修改 production/tracked 文件。

当前只修 synthetic collision 已机械证明的最小缺陷；不因原始 blind #2 的模型身份伪造顺手重做 KnowledgeRetrieve、BKP schema 或模型治理。

## 当前直接下一步

**作者验收修复已完成三轮（2026-08-25 / 2026-08-26 / 2026-08-27）**：第一轮修 acceptance 卡死/设置发现/Qoder 升级合同/DeepSeek 模型能力/素材作者面/UI 视觉；第二轮完成 StoryWrite Interactive 两阶段桥、DeepSeek Harness 通用多 provider 发现、验证式执行审计与素材真实管理/加工工作流；第三轮（本轮）修复作者操作生命周期与 HCI 模型——AI 任务归属 App 级协调器而非页面挂载生命周期（导航/卸载绝不取消/孤立任务），桥请求存储与 /gowrite 激活分离（Direct 永不进入 active.json）、审计 `awaiting_confirmation` 生命周期、正式项目选择跨导航保留、全局任务条/通知 HCI、写作/检查动作语义、素材库与执行记录一致性。自动化回归全绿（后端 287 passed / 2 skipped；UI `tsc -b` + `vite build` PASS；前端纯模型测试 13 passed；frozen Skill 套件保持通过）。阶段状态 = **AUTHOR_ACCEPTANCE_IN_PROGRESS**。

下一真正验证 = **真实作者 acceptance**：作者使用真实模型执行「新建作品 → 规划 → 写作（Direct / Interactive）→ 检查」与「素材导入 → 提纯 → 蒸馏 → 检索」端到端，验证文学质量、工具调用行为与素材加工结果（自动化测试全部使用假 adapter / 假 CLI，零真实模型调用）。修复不声明作者验收完成。

## 下一真正验证

StoryPlan 真实作者纵切通过后：

真实 author acceptance：
- 作者接受正文 → accepted_text
- 正式正文落盘
- 进入正确项目的 production Story State
- 新会话/切换项目后生成正确 next Context

多项目隔离：
- 两部测试作品故意使用同名人物但不同事实
- 来回切换后 State / planning / 正文不得串书
- 两部作品仍可共同调用共享 BKP

不再以 Benchmark / Gate / Phase 编号驱动。以真实创作产出驱动。

## 关键原则

- BKP_POSTHOC_SPARSE_PROBLEM_DRIVEN
- CONTEXT explicit selection, empty selection no whole-State fallback
- CAPABILITY_FIRST_CONSUMER_DRIVEN
- THIN_ORCHESTRATION_BUILD_ALLOWED（仅薄操作层；完整通用 Writer platform 未获批，AI-write 桌面工作台 1.0 除外）
- KNOWLEDGE_SHARED_AUTHORITY_ISOLATED
- 冻结子系统没有真实 consumer blocker 不重开
