# 目录规范（8 条核心原则）

> 本规范约束 AI-Write 项目的目录结构与素材管理。任何脚本、Skill 或协作行为都不得违反以下原则。
> 与目录无关的通用规则见各 Skill 自身文档。

## 原则 1：八目录各司其职，冻结顶层结构

只使用以下 8 个顶层目录，不擅自新增、重命名或合并顶层目录：

`00_项目控制`、`01_原始素材`、`02_原著蒸馏`、`03_作品工程`、
`04_写作知识库`、`05_Skills与自动化`、`06_工作区`、`99_归档`。

确需新增顶层目录时，先与项目维护者确认并同步更新 `README.md` 与本文档。

## 原则 2：人类浏览目录简单直观；复杂 Agent 状态只在 06_工作区

人类日常浏览的目录（`01_原始素材`、`02_原著蒸馏`、`03_作品工程`、`04_写作知识库`）
应保持“一眼能懂”的扁平结构。所有复杂的、易变的 Agent 中间状态（转换缓存、分章、
日志、临时输出）只放在 `06_工作区/**`，**不得**散落到人类浏览目录中。

## 原则 3：第三方原著全文 Local Only，绝不上传 GitHub

`01_原始素材/**` 中的第三方原著（EPUB/TXT/PDF/ZIP/AZW3/MOBI）**只在本地保留**，
不上传 GitHub，避免版权与泄露风险。GitHub 上只放**索引元数据**（格式、状态、SHA256、
处理进度等），不含任何正文全文。该排除由 `.gitignore` 的 `01_原始素材/**/*.{...}` 规则保证。

## 原则 4：作品身份优先于物理目录；合集容器允许“一个目录含多部作品”

- **普通单本作品**仍遵循“每部作品一个物理目录”：`01_原始素材/<分类>/<作品>/`，
  不为“经营/悬疑/宗教/题材”等主题再建一套并列目录（避免同一书分散在多处）。
- **但一个目录 ≠ 一部作品**：当一部“合集/套装”原著在物理上只占一个目录时，
  允许该目录包含多部**逻辑作品**（例如单作者作品合集、多位作者套装）。
- **合集处理**：用脚本按 EPUB 自身的 OPF / spine / NCX 结构把合集拆成多本
  **独立可运行的 EPUB**（拆分基准是文件结构，**不是** LLM 猜测章节边界）；
  原合集永久保留、不修改、不覆盖、不删除。拆分脚本见
  `05_Skills与自动化/01_Skills/SourcePrepare/scripts/epub_collection_split.py`。
- **合集容器目录**内放一个 `collection_manifest.json`（Local Only，标记每本拆分作品的
  `book_id` / 文件名 / SHA256 / 来源容器名）。中央索引 `原始素材清单.csv` 用
  **「来源容器」列**记录“该文件来自哪个合集”，而不是把整个合集当成“一整部作品”。
- **作品身份以 `book_XXXX` + 中央索引为准，不以文件夹名决定**：同一作品的不同来源
  （例如旧目录里的单本 + 合集里拆出的同名单本）靠 `book_id` 归并；
  SourcePrepare 能跨目录识别并交叉校验该作品的全部候选来源。

## 原则 5：稳定作品ID（book_XXXX），文件名/分类/容器变动不改变 ID

每部作品分配稳定 ID `book_0001`、`book_0002`……，按“分类顺序 + 作品名”排序分配，
不因格式（EPUB→TXT）、版本、文件名微调、**分类平移**（如 网络小说→中文文学）或
**合集拆分入库**而改变。`index_builder.py` 的 `discover()` 会复用中央索引里既有 ID，
新作品才顺序分配下一个 `book_XXXX`，因此已分配的 ID 永远不被重新分配或回收。
下游（`06_工作区/SourcePrepare/<ID>_<作品>/`、`02_原著蒸馏/<ID>_<作品>/`）一律用作品ID 关联，
不依赖原始物理路径。

## 原则 6：原始素材只读，SourcePrepare 不破坏源文件

SourcePrepare 对 `01_原始素材` 是**只读**的：不覆盖、不重命名、不删除、不在原地转换。
所有转换产物都写到 `06_工作区/SourcePrepare/`。检测到源文件损坏时只标记 `FAIL`/`REVIEW`，
绝不修改或“修复”原文件。

## 原则 7：一类作品一个分类；专业资料不当书籍处理

`01_原始素材` 下只使用 6 个分类目录（网络小说 / 中文文学 / 外国文学 / 历史与古代资料 /
现代专业资料 / 其他参考资料）。`05_现代专业资料` 是非书籍类专业资料（报告、标准、资料汇编），
SourcePrepare 对其标记 `NOT_APPLICABLE`，不生成 `06_工作区` 转换产物，不进入 BookDistill。
待人工归类的素材先放 `06_其他参考资料/00_待核验/`，SP 不自动处理。

## 原则 8：索引是唯一 GitHub 可见的素材真相来源，SP 跑完自动回写

`00_项目控制/原始素材清单.csv` 与 `00_项目控制/原始素材总索引.md` 是素材状态的唯一权威，
远程协作者通过它们了解“本地有什么、什么格式、处理到哪一步”。每次 SourcePrepare 跑完一部作品，
必须自动回写该作品的状态/字符数/章节数（复用 `scripts/index_builder.py` 的 `update_book()`），
保证索引与本地实际处理进度一致。

## 原则 9：知识文档带 YAML frontmatter，兼容 Obsidian（但不在本仓库部署 Obsidian）

`04_写作知识库`、`02_原著蒸馏`、`03_作品工程` 下的人工/生成文档，首选在文件头部带
YAML frontmatter（至少含 `title`、`tags`、`source` 等字段），以便未来在 Obsidian 等
双链笔记软件里直接索引、反链、检索。

- **仅约定 frontmatter 规范，不引入 Obsidian 依赖、不新建顶层目录、不提交 `.obsidian/` 配置。**
- 文档正文用普通 Markdown，不依赖任何 Obsidian 专属语法（如 `![[...]]` 嵌入）以保证通用可读。
