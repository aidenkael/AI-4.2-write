# AGENTS.md（项目级规则）

> 本文件记录本项目的固定协作规则，供 WorkBuddy / Codex 等智能体遵循。
> 总体守则见用户全局记忆；本项目特有规则只写在这里，不塞进总体守则。

## 一、过程性报告的存放位置（重要）

由智能体在任务中**单独生成的过程性报告**（如 SourcePrepare 生产验证报告、素材扫描/整理/入库报告等）：

- **不要放进 `00_项目控制/`**（该目录只放项目控制用的清单、索引、规范等长期资产）。
- **统一放到 `E:\AI-Write\.workbuddy\memory\`**。
- `.workbuddy/` 已在 `.gitignore` 中忽略，**不进入 GitHub**（本地保留，避免上传第三方全文或内部过程文件）。
- 只在 `00_项目控制/` 保留用户明确要求的、需要纳入版本控制的正式资产。

## 二、Git 与敏感/本地数据边界

- `01_原始素材/`、`06_工作区/`、`05_Skills与自动化/pandoc/`、`.workbuddy/` 均已在 `.gitignore` 忽略，**不得上传 GitHub**。
- 提交前确认只包含预期文件；若工作区出现用户已本地删除的文件，用 `git rm --cached` 同步删除到索引并提交，再推送远程。

## 三、改动范围纪律

- 只修改当前任务必需内容，不擅自重构、扩展或破坏稳定功能。
- 复杂任务分阶段执行、写入并测试；中断后从实际进度继续。
- 业务规则以当前项目最新文件或用户本次说明为准，动态参数不要写死。
