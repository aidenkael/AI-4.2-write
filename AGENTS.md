# AI-write Agent 长期规则

本文件面向进入仓库工作的 Codex / Agent。项目事实与目录规则仍以 `00_项目控制/README_目录使用说明.md` 及对应 STATUS/协议为准；本文件只保存跨任务长期约束。

## 1. 项目定位

AI-write 当前是作者个人、私人使用的小说写作工作台。现阶段目标是吸收成熟上游能力，辅助真实长篇小说创作；不是对外销售软件、公开分发整套工作台或提供 SaaS。

## 2. 开源上游使用原则

不要因为“必须原创”而重复造轮子。只要许可证允许当前私人用途，可以积极：

- clone / 下载上游项目；
- 复制需要的代码、Prompt、Skill、规则与工作流；
- 汉化、修改、删减、组合、重构；
- 经过 Benchmark 验证后固化为 AI-write 本地 Skill / 能力；
- 后续写作优先调用本地固化版本，不要求每次访问 GitHub 或原作者服务。

GitHub 上游是能力与工程经验来源，不应成为未来运行时必须联网依赖的一组外部服务。

## 3. 来源与许可证记录

对任何实际复制或修改的上游，至少保留：

`来源仓库 | 上游 commit/tag | LICENSE | 复制/修改范围`

处理原则：

- MIT / Apache-2.0：当前可直接下载、修改、适配、融合，按许可证保留必要声明。
- CC BY-NC-SA 等非商业/ShareAlike 来源：当前私人、非商业研究和本地改造可以纳入候选；必须保留来源与许可证标记。未来若公开、商业化或分发，再重新审核。
- AGPL 等强 copyleft：当前可本地下载、运行、修改、Benchmark；保留来源与许可证，未来若分发软件或提供网络服务，再专门审核相应义务。
- 许可证不明：可以研究思想和公开行为，但在未核实前不要直接复制其受版权保护的代码/Prompt 进入核心。

不要因为 AGPL、CC BY-NC-SA 本身，就在当前私人研究阶段自动排除高价值项目。

## 4. Benchmark 与吸收原则

- 不按 GitHub 项目选“总冠军”，按能力验证。
- 始终保留 strong baseline，防止把模型本来就会的能力误认为 Skill 增益。
- 外部 Skill 可以直接作为候选参赛；通过实测后再决定整套保留、部分吸收、改造或放弃。
- 优先把胜出的机制转化为 AI-write 本地能力，而不是长期依赖上游原项目结构。
- 作者主要判断实际小说问题与创作效果；内部术语、证据、许可证、能力归类由 Controller / Benchmark 处理。

## 5. 商业化边界

当前无需为尚未发生的商业化过度设计限制。

只有当 AI-write 未来出现以下变化时，才触发统一许可证审计：

- 公开发布整套工作台；
- 出售或授权 AI-write 软件；
- 向外提供在线/SaaS 服务；
- 大规模分发修改后的上游项目或衍生 Skill。

届时再根据实际使用的上游逐项决定：保留、履行许可证、取得授权、隔离，或独立重写。

## 6. 执行习惯

开始任务前先确认本地分支与 GitHub `main` 的状态。若用户/Controller 明确要求同步，先安全同步最新 `main` 再执行；如果本地存在未提交修改或同步可能覆盖工作，不得强行 pull，先报告冲突风险。

## 7. Git 同步与本地保护规则

以下规则优先级高于任何自动化同步逻辑：

1. **用户明确确认的意图是最终依据。** 本地工作树与远端发生未知差异时，任何一边都不得自动覆盖另一边；先 `fetch` + `compare`，再由用户/Controller 确认。
2. **用户本地删除 / 移动 / 改名不得自动 restore。** 如果用户已从磁盘删除某个 tracked 文件，Agent 不得因为 GitHub 上仍存在就 `git checkout`、`git restore` 或 `git reset` 把它取回。
3. **未知差异优先 `fetch` + `compare`。** 发现本地与远程不一致时，先 `git fetch origin` 再用 `git log` / `git diff` 对比，而不是直接 pull 覆盖。
4. **pull 前先确认工作树。** 确认当前 branch、未提交修改、stash 状态；如果工作树不干净，先报告风险，不得强行 pull。
5. **禁止未经授权执行以下操作：**
   - `git reset --hard`
   - `git restore .`
   - `git checkout -- .`
   - `git clean -fd`
   - 任何批量 restore / clean 命令
   如确需执行，必须获得用户/Controller 逐项确认。
6. **Local Only / untracked 不得为了整洁自动清理。** 未跟踪文件和 gitignored 目录是用户有意保留或工具生成的，不得以"仓库整洁"为由删除。
7. **用户确认删除 tracked 文件时，必须通过正常 Git 删除并 commit，** 让删除进入项目历史（`git rm` → `commit`），不得只从磁盘移除而不记录。
8. **重大目录调整必须走完整流程：** 审计 → 清单 → 用户/Controller 确认 → 执行。不得跳过确认步骤。
9. **Agent 临时指令与临时反馈默认不进入 `00_项目控制`。** 这类文件应放在 `06_工作区` 对应子目录中；只有经过确认的长期控制信息才可提升到 `00_项目控制`。

## 8. 文件生命周期规则

本规则是当前项目治理规则，可随项目演进由用户/Controller 维护，不视为永久冻结体系。

### `00_项目控制` — 仅保留项目级长期控制文件

准入：项目总体说明、长期规则、全局能力地图、总体路线、长期 Benchmark 方法论、许可证与来源原则、权威且持续更新的项目级索引（如 `原始素材清单.csv`、`原始素材总索引.md`）。

默认不存：单轮 Benchmark base task / runner input / manifest / 单轮污染审计 / Controller 一次性修订要求 / Agent 临时指令 / Agent 临时反馈 / 一次运行产生的报告。

### `06_工作区` — 承载进行中的工作

准入：当前 Benchmark、当前 Skill 调研、实验材料、runner、Controller 工作文件、中间分析、临时报告、尚未验证的机制。

工作完成后，有价值的结论提升到 `00_项目控制` 或 `04_写作知识库`，过程文件归档到 `99_归档`。

### `05_Skills与自动化` — 只存已形成可调用能力的内容

准入：经过验证的 Skill、实际可调用的 Agent / 脚本 / 自动化、运行时组件。

### `04_写作知识库` — 只存已验证的跨作品知识

准入：经过足够验证、可跨作品复用的写作方法 / 规律 / 案例。

### `99_归档` — 存放已结束的历史资料

准入：已完成的 Benchmark 轮次过程记录、被新版替代但仍需追溯的文件、一次性完成但有参考价值的报告。

## 9. 项目阶段门禁与防跑偏规则

任何 Agent 在开始实质任务前，都必须读取并遵守：

`00_项目控制/项目阶段门禁.md`

长期约束：

1. 当前门禁未完成且未经用户明确确认，不得自行进入下一阶段。
2. 单次任务原则上只推进当前门禁中的一个明确子目标；发现额外问题可以记录，但不得“顺手扩展”。
3. 候选池中出现过某项目，不等于该项目已经实际比较或验证；必须显式区分正式实测、轻量实测、代码/架构审阅、未实测。
4. 新 Benchmark、重大目录变化、批量处理、新架构建设、跨门禁动作均需要用户明确授权。
5. 若聊天中的临时建议与 `当前工作索引.md`、`项目推进记忆.md`、`项目阶段门禁.md` 或用户最新明确决定冲突，先停止并重新对齐，不得沿着临时建议继续执行。
