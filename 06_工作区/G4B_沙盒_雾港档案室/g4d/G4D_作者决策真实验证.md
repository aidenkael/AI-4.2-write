# G4-D 作者决策真实验证

> `project_id: g4b-fogharbor-archive`  
> `base_state_rev: 1`  
> `authority: derived_decision_surface_only`  
> `status: CONSUMED / HISTORICAL`  
> 本决策面已被真实作者反馈 `g4d-dec-001` 消费；当前 Story State 为 `state_rev=2`，不得直接复用本文件继续决策。

## 1. 当前创作问题

林昼在夜班发现一份标记为姐姐林晚的封存记忆；林晚官方已死亡三年，而该档案提交日期晚于死亡日期六个月。

当前场景不解释谜底。真正要解决的是：**怎样让林昼从“按流程处理异常”走到“这件事和我有关，我必须做决定”，同时让读者越来越想知道、也越来越担心她会付出什么代价。**

## 2. 三个候选方向

这些都是历史候选，不是 Canon。

### 方向 A｜稳着往里走

林昼先按正常流程处理异常，不直接违规。一次更深但仍合规的元数据核验，让“姐姐最后一次未接来电”的私人刺痛以具体反应冒出来；她最后决定暂不上报，先查清许可链或身份认证的下一层信息，但仍不打开记忆正文。

### 方向 B｜让回避本身变成选择

林昼本来想把异常移交或关掉，不碰姐姐留下的东西；但完成移交前，她必须亲手确认一个关键身份/权限字段。重点放在她怎样克制、拖延、试图结束这件事，最后却主动多查一步。

### 方向 C｜先让读者感觉到代价

林昼仍按流程查，但读者先看到某种“继续深查存在代价”的候选提示，林昼随后才意识到这一点。具体审计/处罚机制当时尚未成为 Canon。

## 3. 实际作者反馈

作者自然表达：当前场景更接近 C，但小说写法、节奏和详略不应被固定模板化。

后台据此生成：

- `decisions/g4d-dec-001.md`
- `diffs/g4d-diff-001.yaml`

并只修改未来 `approved_plan`，未修改 Canon。

## 4. 历史验证规则

- 真实作者反馈才可创建 `author_decision`；
- 改未来场景方案最多更新 `approved_plan`，不得伪装成 occurred Canon；
- `reject_all / defer` 默认不写 Story State；
- 没有已接受正文时不生成 `accepted_text`；
- 模拟测试语句只能验证解析分支，不能生成 authority；
- 当前文件的 `base_state_rev=1` 已过期。

> 未来若继续该沙盒，必须从当前 `state_rev=2` 重新编译 Brief / Context / 决策面。