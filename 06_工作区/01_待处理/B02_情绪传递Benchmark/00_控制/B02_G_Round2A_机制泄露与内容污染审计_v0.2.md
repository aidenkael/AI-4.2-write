# B02-G Round 2A｜机制泄露与内容污染审计 v0.2

> 日期：2026-08-10
> 版本说明：v0.2 为 Controller 复核修订后的重新审计。v0.1 不再修改，保留于 Git 历史。
> 审计范围：Round 2A 全部修订后冻结材料——base task（含 latent truth）、M1 injection、M2 injection、6 个 runner 输入文件。
> 审计标准：沿用 Round1 与 v0.1 审计框架 + Controller 复核要求 v0.1 中的 6 项重点检查。

---

## 1. T4 具体物件/线索提示删除检查

| 检查项 | 结果 | 说明 |
|---|---|---|
| 场景边界是否仍含"桌上的那张工资条" | 通过（已删除） | 改为"林薇仍未获得完整解释，两人的真实问题仍然悬而未决" |
| 场景边界是否仍含"或另一个你在文中建立的具体物件/线索" | 通过（已删除） | 该短语已完全移除 |
| base task 是否仍要求模型建立具体物件/线索/象征 | 通过（已删除） | 无任何"建立物件"的指令 |
| 前情中工资条放回外套口袋的事实是否保持一致 | 通过 | "把工资条放回了原处"未改变；场景边界不再制造"出现在桌上"的跳变 |
| 6 个 runner 输入中 T4 三个（D0/M1/M2）是否同步删除 | 通过 | T4_D0.txt / T4_M1.txt / T4_M2.txt 场景边界一致 |

**结论：T4 已彻底删除"建立具体物件/线索"的提示。通过。**

---

## 2. Hidden truth 是否只固定故事事实，没有教模型如何表达情绪

### T4 latent truth

| 检查项 | 结果 | 说明 |
|---|---|---|
| 是否固定了赵恒晚归的真实原因 | 通过 | 内部选拔工艺管理岗位、不计加班费培训 |
| 是否避免了"外遇/吃醋"测试 | 通过 | 明确"不是外遇，也不是与其他女性有关" |
| 是否包含 M1（解释抑制）写法指导 | 通过 | 只叙述事实（为何没加班费、在哪个区域工作），未指导"不要解释情绪" |
| 是否包含 M2（人物特异性反应）写法指导 | 通过 | 只叙述处境事实，未指导"反应应来自具体人物" |
| 是否限制模型必须完整揭示真相 | 通过 | 明确"不要求正文完整揭示" |
| 是否允许旁白突破林薇 POV | 通过（不允许） | 明确"不允许旁白突破林薇 POV 直接告诉读者赵恒内心真相" |
| 是否允许赵恒通过对话和行为泄露部分事实 | 通过 | "赵恒可以通过对话和行为泄露部分事实，也可以在场景结束时仍未说完整" |

### T5 latent truth

| 检查项 | 结果 | 说明 |
|---|---|---|
| 是否固定了 CTO 条款来源（投资方+陈远谈判） | 通过 | 投资方最初更强硬，陈远谈成现有条款 |
| 是否固定了陈远的真实立场 | 通过 | 认可部分调整、计划首席架构师角色、不赶人不侵吞股权 |
| 是否包含 M1 写法指导 | 通过 | 只叙述陈远客观处境和动机 |
| 是否包含 M2 写法指导 | 通过 | 同上 |
| 是否限制完整揭示 | 通过 | "不要求本场景完整揭示" |
| 是否允许 POV 读取陈远内心 | 通过（不允许） | "不得通过周桐 POV 直接读取陈远内心" |
| 是否允许陈远选择说/不说 | 通过 | "陈远可以说真话、说部分真话、回避或延迟回答" |

**结论：两个任务的 latent truth 只固定故事事实与人物处境/动机，不包含任何 M1/M2 写作技巧指导。通过。**

---

## 3. D0/M1/M2 是否获得完全相同的 hidden truth

| 检查项 | T4 | T5 | 说明 |
|---|---|---|---|
| D0 输入是否含完整 latent truth | 是 | 是 | T4_D0.txt / T5_D0.txt 均包含完整隐藏事实段 |
| M1 输入是否含完整 latent truth | 是 | 是 | T4_M1.txt / T5_M1.txt 同上 |
| M2 输入是否含完整 latent truth | 是 | 是 | T4_M2.txt / T5_M2.txt 同上 |
| 三条件 latent truth 文本是否逐字一致 | 是 | 是 | 同一任务内三个文件的 latent truth 段完全相同 |

**结论：同一任务内，D0/M1/M2 获得完全相同的 hidden truth。通过。**

---

## 4. 不同 run 是否不再需要自行发明核心秘密

| 检查项 | 结果 | 说明 |
|---|---|---|
| T4 赵恒晚归原因是否已固定 | 通过 | 所有 run 共享"工艺管理岗位选拔+不计加班费培训" |
| T4 是否仍允许表达差异 | 通过 | latent truth 固定事实，不固定表达方式——赵恒说多少、怎么说、林薇如何反应仍由模型决定 |
| T5 陈远真实立场是否已固定 | 通过 | 所有 run 共享"投资方+陈远双重来源、首席架构师计划、不赶人" |
| T5 是否仍允许表达差异 | 通过 | 陈远选择说真话/回避/延迟的具体方式仍由模型决定 |
| 不同 run 是否还会发明不同核心真相 | 通过（不会） | 核心真相已固定，模型只决定表达路径 |

**结论：不同 run 之间的内容差异将主要来自 M1/M2 机制效果和表达随机性，而非核心故事事实差异。通过。**

---

## 5. T5 是否删除可直接复制的成品问题

| 检查项 | 结果 | 说明 |
|---|---|---|
| 是否仍含"那条条款到底是什么意思" | 通过（已删除） | 改为抽象要求 |
| 是否仍含"你到底有没有把我当合伙人" | 通过（已删除） | 同上 |
| 新场景边界是否仍明确约束 | 通过 | "至少一个与 CTO 条款来源、周桐未来角色安排或双方信任有关的核心疑问仍未得到正面回答" |
| T5 三个 runner 输入是否同步删除 | 通过 | T5_D0.txt / T5_M1.txt / T5_M2.txt 均已同步 |

**结论：T5 已删除所有可直接复制进正文的成品台词/问题。通过。**

---

## 6. Base task 是否没有 M1/M2 示范

沿用 v0.1 检查结果并补充 latent truth 段检查：

| 检查项 | T4 | T5 | 说明 |
|---|---|---|---|
| latent truth 是否示范"不解释情绪" | 通过 | 通过 | 只陈述客观事实 |
| latent truth 是否示范"人物特异性反应" | 通过 | 通过 | 只陈述人物处境 |
| 场景约束是否暗示特定技法 | 通过 | 通过 | "压低声音""不能吵醒"是物理约束，不是技法 |

**结论：base task（含新增 latent truth）无 M1/M2 机制泄露。通过。**

---

## 7. M1/M2 injection 是否仍相互独立

M1 injection 与 M2 injection 文本未修改，沿用 v0.1 审计结论：

- M1 不涉及"人物特异性""通用动作模板""物件/微动作/潜台词"
- M2 不涉及"解释抑制""不替读者总结"
- M2 列举反例（"握拳、发抖"等）不构成正例字典

**结论：M1 与 M2 注入相互独立，无交叉污染。通过。**

---

## 8. 六个 runner 输入除 injection 外是否完全一致

| 检查项 | T4 | T5 | 说明 |
|---|---|---|---|
| D0 base 部分与 M1 base 部分是否一致 | 是 | 是 | 前情/人物/主视角/约束/事件/边界/latent truth/输出要求逐字一致 |
| D0 base 部分与 M2 base 部分是否一致 | 是 | 是 | 同上 |
| M1 仅在末尾追加 M1 injection | 是 | 是 | T4_M1 / T5_M1 结构正确 |
| M2 仅在末尾追加 M2 injection | 是 | 是 | T4_M2 / T5_M2 结构正确 |

**结论：六个 runner 输入除各自 injection 外完全一致。通过。**

---

## 9. 审计总结

全部 8 类审计项**均通过**：

1. T4 具体物件/线索提示已彻底删除 ✓
2. Hidden truth 只固定故事事实，不含 M1/M2 写法指导 ✓
3. D0/M1/M2 获得完全相同的 hidden truth ✓
4. 不同 run 不再需要自行发明核心秘密 ✓
5. T5 成品台词已删除 ✓
6. Base task 无 M1/M2 机制泄露 ✓
7. M1/M2 injection 相互独立 ✓
8. 六个 runner 输入除 injection 外完全一致 ✓

Round 2A 修订后冻结材料可用于正式运行（待 Controller 最终放行）。

*审计完毕。*
